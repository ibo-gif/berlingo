# 📱 Berlingo - Инструкция по сборке и установке APK

## 🎯 Краткая инструкция (для нетерпеливых)

```bash
# 1. Клонируйте проект
git clone https://github.com/ibo-gif/berlingo.git
cd berlingo

# 2. Скачайте зависимости
flutter pub get

# 3. Соберите APK
flutter build apk --release

# 4. Установите на телефон
adb install build/app/outputs/flutter-apk/app-release.apk
```

**АПК находится в:** `build/app/outputs/flutter-apk/app-release.apk`

---

## 🔧 Полная пошаговая инструкция

### Требования перед началом

Проверьте что у вас установлено:

```bash
# FlutterSDK
flutter --version

# Dart
dart --version

# Android SDK
flutter doctor -v
```

Если не установлено - установите с [flutter.dev](https://flutter.dev/docs/get-started/install)

### Шаг 1: Получите код репозитория

#### Способ А: Использование Git
```bash
git clone https://github.com/ibo-gif/berlingo.git
cd berlingo
```

#### Способ Б: Скачивание ZIP
1. Перейдите на [GitHub репозиторий](https://github.com/ibo-gif/berlingo)
2. Нажмите зеленую кнопку "Code"
3. Выберите "Download ZIP"
4. Распакуйте архив
5. Откройте терминал в этой папке

### Шаг 2: Подготовка проекта

```bash
# Перейдите в папку проекта
cd berlingo

# Проверьте окружение
flutter doctor

# Установите зависимости
flutter pub get

# Очистите предыдущие сборки (если делали ранее)
flutter clean
```

### Шаг 3: Сборка APK - выберите один вариант

#### Вариант 1: Быстрая сборка (рекомендуется)
```bash
flutter build apk --release
# Результат: build/app/outputs/flutter-apk/app-release.apk
# Размер: ~40-50 MB
# Совместимость: все Android устройства с Android 5.0+
```

#### Вариант 2: Оптимизированная для ARM64 (меньше размер)
```bash
flutter build apk --release --target-platform android-arm64
# Результат: build/app/outputs/flutter-apk/app-arm64-release.apk  
# Размер: ~30-35 MB
# Совместимость: Android 5.0+ с 64-bit процессором (большинство современных)
```

#### Вариант 3: Для Google Play Store
```bash
flutter build appbundle --release
# Результат: build/app/outputs/bundle/release/app-release.aab
# Размер: ~25-30 MB (Google Play оптимизирует для каждого устройства)
```

### Шаг 4: Установка APK на телефон

#### Способ 1: Через ADB (рекомендуется)

```bash
# Убедитесь что телефон подключен
adb devices

# Установите APK
adb install build/app/outputs/flutter-apk/app-release.apk

# Или для ARM64 версии:
adb install build/app/outputs/flutter-apk/app-arm64-release.apk
```

#### Способ 2: Передача файла на телефон

1. Подключите телефон к компьютеру через USB кабель
2. На компьютере откройте файловый менеджер
3. Перейдите в: `build/app/outputs/flutter-apk/`
4. Найдите файл `app-release.apk`
5. Скопируйте его на телефон в папку **Downloads**
6. На телефоне откройте **Диспетчер файлов**
7. Перейдите в **Downloads**
8. Нажмите на `app-release.apk`
9. Нажмите "Установить"
10. Подтвердите установку

#### Способ 3: Через облако

1. Загрузите `app-release.apk` в облачное хранилище (Google Drive, Яндекс.Диск, и т.д.)
2. На телефоне откройте приложение облака
3. Скачайте APK файл
4. Откройте APK из папки Downloads
5. Нажмите "Установить"

### Шаг 5: Запуск приложения

1. Найдите иконку Berlingo на главном экране
2. Нажмите на иконку для запуска
3. Выберите языки для изучения
4. Начните с первого раздела
5. Пройдите все 8 уровней! 🎓

---

## 📊 Размеры файлов

| Вариант | Размер | Совместимость |
|---------|--------|---|
| app-release.apk | 40-50 MB | Все Android (5.0+) |
| app-arm64-release.apk | 30-35 MB | ARM64 устройства |
| app-release.aab | 25-30 MB | Google Play Store |

---

## 🐛 Устранение проблем

### Проблема: "flutter: command not found"
```bash
# Flutter не в PATH
# Добавьте Flutter в путь:
export PATH="$PATH:/path/to/flutter/bin"

# Или переустановите Flutter
```

### Проблема: "Android SDK not found"
```bash
# Убедитесь что установлен Android SDK
flutter doctor --android-licenses

# Принимайте все лицензии
yes | flutter doctor --android-licenses
```

### Проблема: "Device not found" при установке через ADB
```bash
# Проверьте что телефон подключен
adb devices

# Включите режим разработчика на телефоне:
# Настройки > О телефоне > Номер сборки (нажмите 7 раз)
# Затем включите USB отладку в:
# Настройки > Параметры для разработчиков > USB отладка
```

### Проблема: "Target has required Android API level X"
```bash
# Обновите Android SDK в Android Studio
# Или используете командную строку:
flutter run -d all
```

### Проблема: Ошибка при сборке
```bash
# Очистите всё и начните заново
flutter clean
flutter pub get
flutter build apk --release
```

---

## 🎮 Использование приложения

### Главный экран
- Выберите языки нажатием на флаги
- Нажмите на выбранный язык в разделе "Ваши курсы"

### Экран разделов
- Вы видите 8 разделов
- Раздел 1 всегда открыт
- После прохождения раздела открывается следующий
- ✅ = пройден, 🔒 = закрыт

### Экран урока
- Читайте вопрос
- Выбирайте ответ из 4 вариантов
- ✅ Зеленый = правильно
- ❌ Красный = неправильно
- Читайте объяснение
- Переходите к следующему вопросу

### Система прогресса
- Каждый пройденный раздел = +100 XP
- Уровень показывает текущий раздел (1-8)
- Опыт накапливается

---

## 📱 Системные требования

**Минимум:**
- Android 5.0 (API 21)
- 50 MB свободного места
- Оперативная память: 1 GB

**Рекомендуется:**
- Android 7.0+ (API 24+)
- 100 MB свободного места
- Оперативная память: 2+ GB

---

## 🚀 Дополнительные команды Flutter

```bash
# Запуск в debug режиме (для разработки)
flutter run

# Запуск с подробными логами
flutter run -v

# Список подключенных устройств
flutter devices

# Очистка кэша
flutter clean

# Обновление зависимостей
flutter pub upgrade

# Проверка на ошибки
flutter analyze

# Форматирование кода
dart format .

# Создание profile сборки (для профилирования производительности)
flutter build apk --profile
```

---

## 📞 Если что-то пошло не так

1. **Прочитайте ошибку внимательно** - часто ответ в сообщении об ошибке
2. **Выполните `flutter clean` и начните заново**
3. **Проверьте `flutter doctor`** - убедитесь что всё установлено
4. **Обновите Flutter и Dart** - `flutter upgrade`
5. **Создайте Issue на GitHub** - описывайте подробно проблему

---

## ✅ Проверочный список

Перед сборкой убедитесь:

- [ ] Flutter установлен и в PATH
- [ ] Dart установлен 
- [ ] Android SDK установлен (API 21+)
- [ ] Телефон подключен (если используете adb)
- [ ] USB отладка включена на телефоне
- [ ] Хватает свободного места (~500 MB для сборки)
- [ ] Интернет соединение стабильно

---

## 🎓 Что дальше?

1. ✅ Установили приложение
2. 🎮 Начни учить языки!
3. 📚 Пройди все 8 уровней
4. 🌍 Выбери другой язык
5. 🚀 Помоги в разработке - добавляй новые языки!

Подробнее читайте в [README.md](README.md) и [DEVELOPER.md](DEVELOPER.md)

---

**Удачи в учении! 🎓📱**
