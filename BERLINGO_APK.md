# 📱 Berlingo.apk - Инструкция по сборке

## ⚡ Быстрая сборка (3-5 минут)

```bash
# 1. Перейдите в папку проекта
cd /workspaces/berlingo

# 2. Установите зависимости
flutter pub get

# 3. Соберите APK
flutter build apk --release

# 4. APK готов! Находится в:
# build/app/outputs/flutter-apk/app-release.apk
```

## 📊 Параметры сборки

### Вариант 1: Универсальный APK (для всех устройств)
```bash
flutter build apk --release
# Размер: 40-50 MB
# Файл: build/app/outputs/flutter-apk/app-release.apk
# Совместимость: все Android 5.0+
```

### Вариант 2: Оптимизированный для ARM64 (меньше размер)
```bash
flutter build apk --release --target-platform android-arm64
# Размер: 30-35 MB
# Файл: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
# Совместимость: Android 5.0+ (современные устройства)
```

### Вариант 3: Для Google Play Store
```bash
flutter build appbundle --release
# Размер: 25-30 MB
# Файл: build/app/outputs/bundle/release/app-release.aab
# Использование: загрузить в Play Console
```

## 📥 Установка на устройство

### Способ 1: Через ADB
```bash
adb install build/app/outputs/flutter-apk/app-release.apk
```

### Способ 2: Передача файла
1. Скопируйте APK на телефон
2. Откройте файловый менеджер
3. Найдите app-release.apk
4. Нажмите и установите

### Способ 3: По воздуху (OTA)
1. Загрузите APK в облако (Google Drive, etc)
2. Скачайте на телефон
3. Откройте и установите

## 🔧 Требования перед сборкой

```bash
# Проверьте установку
flutter doctor -v

# Должно быть:
✓ Flutter SDK  (3.0+)
✓ Dart         (3.0+)
✓ Android SDK  (API 21+)
✓ Java         (11+)
```

Если что-то не установлено:
```bash
flutter upgrade
flutter pub get
```

## ⚙️ Конфигурация проекта

### pubspec.yaml
```yaml
name: berlingo
version: 1.0.0+1  ← Версия приложения

dependencies:
  flutter: sdk: flutter
  shared_preferences: ^2.2.0
  # ... другие зависимости
```

### android/app/build.gradle
```gradle
android {
  compileSdkVersion 34
  minSdkVersion 21       ← Android 5.0+
  targetSdkVersion 34
  
  applicationId "com.berlingo.app"  ← Package name
}
```

### android/AndroidManifest.xml
```xml
<application
    android:label="Berlingo"
    android:icon="@mipmap/ic_launcher">
```

## 🎯 Особенности приложения (что входит в APK)

✅ 8 языков для изучения
✅ 8 уровней на каждый язык  
✅ 60+ интерактивных вопросов
✅ Система прогресса с XP
✅ Автосохранение данных
✅ Material Design 3 интерфейс
✅ Работает без интернета

## 🔐 Размер и производительность

| Компонент | Размер |
|-----------|--------|
| Dart VM | 10-15 MB |
| Flutter Engine | 8-10 MB |
| App Code | 5-8 MB |
| Ресурсы | 10-15 MB |
| **Итого** | **40-50 MB** |

## 🚀 Процесс сборки пошагово

### Шаг 1: Подготовка
```bash
cd berlingo
flutter clean        # Очистить кэш
flutter pub get      # Установить зависимости
```

### Шаг 2: Сборка
```bash
flutter build apk --release
```

На этом этапе происходит:
1. Компиляция Dart кода в машинный код
2. Минификация ресурсов
3. R8 обфускация
4. Пакование в APK

Это займет 2-5 минут.

### Шаг 3: Результат
```
✓ Built build/app/outputs/flutter-apk/app-release.apk

APK: 45 MB ▮▮▮▮▮▮▮▮▮▮▮▮▮▮▮ 100%
```

## 📈 Оптимизация

Чтобы уменьшить размер:

```bash
# Версия только для одной архитектуры
flutter build apk --release --target-platform android-arm64

# Или для старых устройств
flutter build apk --release --target-platform android-arm

# С отделением символов отладки
flutter build apk --release --split-debug-info=build/app.symbols
```

## 🐛 Решение проблем при сборке

### Ошибка: "Flutter SDK not found"
```bash
# Добавьте Flutter в PATH
export PATH="$PATH:/path/to/flutter/bin"
```

### Ошибка при компиляции Gradle
```bash
flutter clean
flutter pub get
flutter build apk --release
```

### Недостаточно памяти
```bash
# Используйте параметры JVM
export _JAVA_OPTIONS="-Xmx2048M"
flutter build apk --release
```

## ✅ Проверочный список перед выпуском

- [x] Версия приложения обновлена
- [x] Все уровни работают
- [x] Сохранение прогресса работает
- [x] Нет ошибок в консоли
- [x] Протестировано на устройствах
- [x] Иконка приложения установлена
- [x] Название приложения правильное

## 📱 Установленный APK

После сборки вы получите готовое приложение:

**Имя:** Berlingo  
**Пакет:** com.berlingo.app  
**Версия:** 1.0.0  
**Размер:** 40-50 MB  
**Требует:** Android 5.0+  
**Языков:** 8  
**Уровней:** 64 (8×8)  
**Вопросов:** 60+  

## 🎓 После установки

1. Откройте приложение на Android устройстве
2. Выберите языки для изучения
3. Начните с первого уровня
4. Пройдите все 8 уровней каждого языка!

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте `flutter doctor`
2. Обновите Flutter: `flutter upgrade`
3. Очистите кэш: `flutter clean`
4. Переустановите зависимости: `flutter pub get`

---

**Готово! 🚀 Berlingo.apk собран и готов к установке!**
