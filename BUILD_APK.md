# Berlingo APK Build Guide

## 📦 Сборка APK для Android

### Обязательные требования перед сборкой

1. **Flutter SDK установлен**
   ```bash
   flutter --version
   ```

2. **Android SDK установлен** (минимум API уровень 21)
   ```bash
   flutter doctor
   ```

3. **Git репозиторий клонирован**
   ```bash
   git clone https://github.com/ibo-gif/berlingo.git
   cd berlingo
   ```

## 🚀 Шаги сборки

### 1. Получить зависимости
```bash
flutter pub get
```

### 2. Запустить на эмуляторе/устройстве (для тестирования)
```bash
# Убедитесь, что эмулятор запущен или устройство подключено
flutter run
```

### 3. Собрать Release APK

#### Вариант 1: Сборка универсального APK (все архитектуры)
```bash
flutter build apk --release

# Результат: build/app/outputs/flutter-apk/app-release.apk
```

#### Вариант 2: Сборка for ARM64 (меньше размер)
```bash
flutter build apk --release --target-platform android-arm64

# Результат: build/app/outputs/flutter-apk/app-arm64-release.apk
```

#### Вариант 3: Сборка App Bundle для Google Play
```bash
flutter build appbundle --release

# Результат: build/app/outputs/bundle/release/app-release.aab
```

## 📊 Информация о файлах

| Тип | Путь | Использование |
|-----|------|---|
| APK (универсальный) | `build/app/outputs/flutter-apk/app-release.apk` | Прямая установка на любой Android 5.0+ |
| APK (ARM64) | `build/app/outputs/flutter-apk/app-arm64-release.apk` | Для современных устройств |
| App Bundle | `build/app/outputs/bundle/release/app-release.aab` | Загрузка в Google Play Store |

## 📱 Установка APK на устройство

### Способ 1: Через ADB (Android Debug Bridge)
```bash
# Убедитесь, что устройство подключено
adb devices

# Установите APK
adb install build/app/outputs/flutter-apk/app-release.apk
```

### Способ 2: Перетащить на устройство
1. Подключите устройство к ПК через USB (или через облако)
2. Передайте файл APK на устройство
3. На устройстве откройте файловый менеджер
4. Найдите и нажмите на `app-release.apk`
5. Нажмите "Установить"

### Способ 3: QR код
Используйте любой QR генератор для создания ссылки на APK и отсканируйте на устройстве.

## 🔐 Подписание APK (для публикации)

### Создание ключевого хранилища (первый раз)
```bash
keytool -genkey -v -keystore ~/berlingo.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias berlingo_key
```

### Подписание APK с помощью jarsigner
```bash
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore ~/berlingo.keystore build/app/outputs/flutter-apk/app-release.apk berlingo_key

# Затем оптимизируйте с помощью zipalign
zipalign -v 4 build/app/outputs/flutter-apk/app-release.apk app-release-signed.apk
```

## 🐛 Устранение неполадок

### Ошибка: "Flutter SDK not found"
```bash
# Убедитесь, что Flutter в PATH
flutter pub get
```

### Ошибка: "Minimum supported Gradle version is X.X.X"
```bash
# Обновите Gradle
flutter pub upgrade
```

### Ошибка при сборке: "Execution failed for task"
```bash
# Очистите кэш
flutter clean
flutter pub get
flutter build apk --release
```

## 📈 Оптимизация размера APK

### Release сборка уже оптимизирована с:
- ✅ ProGuard/R8 обфускацией
- ✅ Удалением неиспользуемого кода
- ✅ Минификацией ресурсов

### Дополнительные способы уменьшить размер:
```bash
# Сборка с разделением по архитектуре (скачивается нужная версия)
flutter build apk --release --target-platform android-arm64
```

## 📝 Параметры build команды

```bash
flutter build apk [OPTIONS]

Опции:
  --release              Создать сборку для выпуска (оптимизирована)
  --debug                Создать отладочную сборку
  --profile              Создать сборку профиля
  --target-platform      Архитектуры: android-arm, android-arm64, android-x86, android-x86_64
  --obfuscate            Обфусцировать код
  --split-debug-info     Отделить символы отладки
```

## ✅ Проверка перед публикацией

- [ ] Версия приложения обновлена в `pubspec.yaml`
- [ ] Приложение протестировано на множестве устройств
- [ ] Нет консоль-ошибок и предупреждений
- [ ] Иконка и название приложения правильные
- [ ] Все разделы (8 уровней) работают корректно
- [ ] Система сохранения прогресса работает

## 📚 Полезные ссылки

- [Flutter Build Documentation](https://flutter.dev/docs/deployment/android)
- [Google Play Console](https://play.google.com/console)
- [Android Developers Guide](https://developer.android.com/)

---

**Готово! Ваше приложение Berlingo APK собрано! 🎉**
